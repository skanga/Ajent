# Ajent parity ledger

Ajent targets behavioral parity with AgenTTY commit
`c7594d64020cfdacb10b6a0b2074bcedcc827bba`. The pinned dependency commits
are recorded in the machine-readable manifests under
`ajent-parity/src/test/resources/reference/`.

## Evidence rules

- Raw reference artifacts are retained and hashed before any normalization.
- JSON comparison requires exact field presence, types, values, and array
  order. JSON object key order is ignored.
- The only standing normalization allowance covers generated timestamps,
  generated identifiers, the declared program-name difference, and declared
  workspace/data-root prefixes.
- Every additional normalized or deliberate difference requires a ledger row
  with evidence and review. “Semantically similar” is not parity.
- A feature remains incomplete until its reference capture, Java test,
  implementation, differential result, and documentation evidence are all
  present.

## Reference baseline

| Item | Evidence | Status |
|---|---|---|
| AgenTTY source | pinned commit in `capture-manifest.json` | frozen |
| Maya | `8c655268272b416faed1ba13ffb6d36c292415ed` | populated |
| acp-cpp | `d8b80082f021fe15a081ddd9fe812667f9435ade` | populated |
| mcp-cpp | `f87d78aa5e031cb80257692b3379805d54e54ca5` | populated |
| Reference tests/probes | all 53 rows in `test-manifest.json` | inventoried; 12 deterministic suites ported from source |
| Reference executable | Windows 0.2.8 binary SHA-256 in `capture-manifest.json` | verified |
| JDK 25 | user `JAVA_HOME`/`PATH` and project-local Maven toolchain select `C:\lang\jdk-25` | `java -version`, `mvn --version`, and `mvn test` green |
| Native suite | source is pinned; POSIX-only probes require Linux CI | deferred to cross-platform CI |

## Feature ledger

| Surface | Reference fixture | Java test | Implementation | Differential result | Status |
|---|---|---|---|---|---|
| CLI and configuration | help/version/invalid raw fixtures plus `src/runtime/main.cpp` dispatch, `skills.cpp::cmd_skills`, and `airgap/airgap.cpp` | `AjentCliTest`, `AuthCommandsTest`, `SkillCommandsTest`, `McpServeCommandTest`, `AcpCommandTest`, `AirgapCommandTest`, `ProcessSandboxTest`, `CliParityTest` | exact parser/help/version/invalid handling; testable top-level command seam; working API-key/OAuth `login`, secret-safe `status`, idempotent `logout`, six-root `skills` inventory with source/path/description/resources/model-visibility, full spec warnings, empty-install guidance, and CI exit status; operational `mcp-serve` and `acp` with native workspace validation, protocol isolation, sandbox lifecycle, settings/provider credential precedence, Ask-default profile validation, and keyless-local prompt authorization; operational `airgap` with reverse dynamic SOCKS SSH, native liveness ordering and exit propagation, terminal markers, explicit and synthesized X11/Wayland clipboard relay, fail-fast three-step credential setup, complete trust-boundary help, and no-PTY Zed ACP configuration generation | help/version/invalid output exact after `agentty` → `ajent` name substitution; auth, skills, MCP, ACP, and air-gap deterministic behavior translated; generated Zed arguments and help compared with the provided Windows binary; full reactor and compiled Java 25 ACP stdio launch green | partial — interactive TUI and interactive settings/provider switching remain |
| Pure FSM/model/edit/scheduler contracts | pinned C++ tests | translated unit tests, including all 13 scheduler cases | implemented | source assertions translated; native CTest confirmation pending | partial |
| Thread/settings persistence | `persistence.cpp` and `store.hpp` source schemas | `ThreadStoreTest`, `SettingsStoreTest`, `AsyncThreadWriterTest`, `FilePersistencePortTest`, lifecycle coverage in `AgentLoopTest`, expanded `ConversationTest` | complete persisted conversation shape, typed load errors, legacy status migration, metadata-only walk, atomic thread/settings writes, exact settings ordinals/omissions, coalescing background writer, concrete runtime filesystem adapter, and effect-worker-then-writer drain ordering on loop shutdown | source assertions and JSON schema translated; native binary differential capture remains | partial |
| Credential v1 envelope and OAuth lifecycle | `cred_crypt.cpp` fixed HKDF/AES-GCM contract, `auth.cpp` path/precedence/PKCE/refresh/command rules, and Anthropic OAuth constants | `CredentialCryptTest`, `CredentialStoreTest`, `CredentialResolverTest`, `CredentialPathsTest`, `MachineSeedTest`, `AnthropicOAuthClientTest`, `AnthropicOAuthLoginTest`, `AuthCommandsTest`, `CredentialOAuthRefreshPortTest`, runtime OAuth cases in `AgentReducerTest`/`AgentLoopTest` | authenticated v1 envelope, tamper rejection, legacy plaintext migration, private atomic writes, CLI/environment/saved precedence, non-blocking expired-OAuth handoff, AgenTTY config path and Windows MachineGuid/user seed; exact Claude public-client authorization URL, S256 PKCE challenge, scopes, callback, state, joined `code#state` handling, and authorization-code/refresh forms over bounded JDK HTTP; typed network/bad-response/API/missing-token errors; working API-key and OAuth login with browser handoff and persisted expiry, secret-safe environment/saved status with every expiration variant, and idempotent logout; refresh-token fallback, expiry calculation, best-effort encrypted persistence, in-memory install, and parked-stream lifecycle | fixed cryptographic vector matches OpenSSL contract; loopback HTTP proves both code-exchange and refresh request/response shapes; command tests prove saved results and non-disclosure; OAuth host override, in-TUI login modal, and native binary differential capture remain | partial |
| Anthropic provider | `provider/anthropic/transport.cpp`, tool-result budget, image, cache-pinning, adaptive-thinking replay, system-prompt, SSE event, header, beta, request-body, cancellation, and liveness contracts | `AnthropicMessagesTest`, `AnthropicStreamDecoderTest`, `AnthropicWireTest`, `AgentSystemPromptTest`, `LiveProviderFactoryTest`, Anthropic and stalled-stream loopback cases in `ProviderHttpTransportTest`, `HttpProviderPortTest` | message-block serializer; 64 KiB UTF-8-safe tool-result budget; last-two-message and last-tool cache pinning; thinking-enabled verbatim signed-thinking replay; hosted action/edit/shell/output/context/repository/DSL/memory behavioral prompt with platform-specific environment notes, bounded user/project/local CLAUDE.md tiers, durable learned-memory projection, and lazy skills catalog; call-time request composition with exact 23-tool catalog, model-specific output ceiling and override, effort/auth, and default model; exact typed OAuth/API-key headers, model/auth/tool-sensitive beta selection, OAuth preamble, system cache block, metadata identity, and adaptive-thinking request fields; byte-incremental SSE decoding for start, heartbeat, text close, thinking/signature, tool JSON, four-counter usage, all native stop reasons, forward-compatible unknown frames, mid-tool EOF repair, and exactly-once terminal events; live bounded JDK HTTP streaming with typed HTTP errors and Retry-After; cancellable asynchronous header wait and virtual-thread body watchdog that closes blocked reads within the 50 ms poll window or after the native 90-second byte-idle limit without duplicating terminal events; call-time runtime routing that consumes refreshed configuration and completes real `AgentLoop` turns | translated source assertions, bounded prompt/request fixtures, and loopback byte-split/stalled/end-to-end streaming are green; Anthropic model listing, persisted settings/credential resolution at process startup, and native binary differential capture remain | partial |
| OpenAI-compatible providers | `openai_transport_test.cpp` assertions plus provider-selection/weak-model branches in `main.cpp` and `cmd_factory.cpp` | `OpenAiWireBuildersTest`, `OpenAiRequestTest`, `ProviderHttpTransportTest`, `ProviderModelCatalogTest`, `ProviderRegistryTest`, `LiveProviderFactoryTest`, `OpenAiStreamParserTest`, `OpenAiStreamDecoderTest`, `HttpProviderPortTest`, wire-framer tests | presets/auth resolution, exact request shape, JDK HTTP streaming, model listing, SSE/native parsing, byte-incremental decoding, salvage, Retry-After, pre-header and blocked-body cancellation, exactly-once termination, 90-second byte-idle recovery, and per-call runtime routing; shared live request composition selects compatible versus native Ollama dialect, injects the concise local prompt, carries probed context windows, and switches weak Ollama models to JSON protocol while hiding skill/memory footguns | all enabled source assertions and provider-routing cases translated and loopback integration green; persisted settings/credential resolution at process startup, a separately worded OpenAI-local prompt, native differential capture, and full retry-class table remain | partial |
| Ollama provider | `ollama_transport_test.cpp` assertions | `OllamaWireTest`, `OllamaRequestBodyTest`, `OllamaStreamParserTest`, `OllamaStreamDecoderTest`, `ProviderHttpTransportTest`, `ProviderModelCatalogTest` | native/JSON-protocol history and request bodies, exact slim prompt and memory tiers, environment options, model capability/context probing, structured/salvaged calls, response pseudo-tool, JDK HTTP and byte-incremental NDJSON | all 39 source scenarios translated and loopback integration green; native differential capture remains | partial |
| Native tool catalog/dispatch | pinned `mcp-cpp` argument/filesystem/search/git/host/memory/web sources, web tests, AgenTTY `spec.hpp`/`mcp_tools_bridge.cpp`, and `toolset_e2e_test.cpp` | family suites plus `ToolCatalogTest`, `NativeToolWireCatalogTest`, `ToolDispatcherTest`, `ToolRuntimeFactoryTest`, `ToolRuntimeTest`, `JdkWebTransportTest`, `ProcessSandboxTest`, edit matcher/repair tests | all 23 catalog bodies and one exhaustive catalog-backed dispatcher; exact standalone MCP descriptions/input schemas/annotations in source recall-bias order, kept distinct from the stricter runtime permission catalog; a production composition root for filesystem/process/search/map/git, durable memory, six-root skills, document retrieval, todos, optional subagents, and bounded redirect-following JDK HTTP; platform process sandbox selection/wrapping; HTTPS fetch and three-engine search | every catalog body routes through Java and the complete validated wire catalog plus concrete runtime are used by `mcp-serve`; provider-backed subagent installation, idle-timeout/output-spill parity, richer grep context, cache parity, and native differential capture remain | partial |
| Permissions and scheduling | exhaustive `policy.hpp`, `effects.hpp`, `spec.hpp`, and 13-case `scheduler_path_test.cpp` | `ToolPolicyTest`, `ToolCatalogTest`, `OutputBudgetTest`, `ToolSchedulerTest` | exact 48-cell permission matrix/reasons, 23-tool ordered capability catalog, task scheduling override, coarse and path-aware parallel scheduling, UTF-8-safe output budgets | source assertions translated; reducer permission lifecycle and native differential capture remain | partial |
| Skills and memory | pinned `mcp-cpp` host/memory shells, AgenTTY `memory_store.cpp`, `skills.cpp`, `skills_engine_test.cpp`, and toolset e2e cases | `HostToolsTest`, `MemoryToolsTest`, `JsonlMemoryStoreTest`, `MemoryPromptTest`, `SkillEngineTest`, `KnowledgePipelineTest`, `AgenttyDocRetrieverTest`, `AgentSessionFactoryTest` | typed host protocols; legacy-compatible durable JSONL with corruption handling, UTF-8 caps, Jaro dedup, supersede/rollover/atomic mutation, and bounded prompt projection; six-root Agent Skills discovery/precedence, lenient frontmatter and block scalars, metadata, hidden skills, bounded tier-3 resources, read-only sandbox allowlisting, catalog/activation payloads, activation dedup, lint, concrete host resolver, and lazy searchable skill/memory sources; live ACP sessions install the shared durable backends and hosted/local prompt projection | concrete backends are installed in production ACP sessions; interactive TUI composition remains | partial |
| RAG and repository map | pinned `mcp-cpp` repository map plus AgenTTY `rag_test.cpp`, `rag_hnsw_test.cpp`, `rag_expand_test.cpp`, `rag_rerank_test.cpp`, `knowledge_test.cpp`, and advanced stemming/semantic/MMR/confidence/hot-reload/cache/MCP/neural cases | `RepoMapToolsTest`, `RagCoreTest`, `HnswIndexTest`, `EmbeddingClientTest`, `RagQueryExpanderTest`, `RagRerankerTest`, `NeuralRerankerTest`, `RagStemmerTest`, `RagCorpusFilesystemTest`, `KnowledgePipelineTest`, `AgenttyDocRetrieverTest` | repository PageRank map; line-aligned semantic chunks and contextual hybrid BM25+dense retrieval with RRF, exact brute-force below 2,000 chunks and pure-Java HNSW above it; native little-endian graph/cache serialization and Windows file-clock metadata; bounded Ollama `/api/embed` batches with graceful lexical fallback and AgenTTY environment resolution; opt-in Porter normalization and generative multi-query expansion; deterministic feature reranking plus bounded-wave opt-in neural scoring, both through concrete JDK Ollama adapters; extractive compression, MMR, contexts/confidence; provenance-stamped multi-source router, filters, normalization, composable pipeline, and canonical `search_docs` funnel | lexical/dense retrieval, HNSW recall and persistence, filesystem indexing, expansion, source fusion, knowledge routing, and optional neural `search_docs` stages ported; full interactive composition and native differential capture remain | partial |
| Agent loop/reducer | `domain/session.hpp` phase/retry proofs, `domain/conversation.hpp` tool-state timestamps, `provider/error_class.hpp`, `tool/util/partial_json.cpp`, `runtime/app/update` stream/tool/compaction lifecycle, `cmd_factory.cpp` wire projection, scheduler, doom-loop, and salvage-dedup sources | `ConversationTest`, `SessionPhaseTest`, `ActiveTurnTest`, `ProviderErrorPolicyTest`, `PartialJsonTest`, `ConversationWireTest`, `AgentReducerTest`, `AgentLoopTest`, `HttpProviderPortTest`, `DispatcherToolPortTest`, `ToolSchedulerTest`, `DoomLoopBreakerTest`, `SalvagedCallDeduplicatorTest` | sealed four-phase typestate with active context absent from idle and exact transition legality; timestamped sealed tool statuses that retain monotonic start/finish invariants across pending, approval, execution, completion, failure, rejection, and cancellation; pure immutable submit/stream/tool/permission/cancel/compact/tick reducer with stale-turn rejection, UTF-8 8 MiB caps, typed effects, catalog-backed tool adapter, path-aware parallel scheduling, and a virtual-thread effect interpreter reusable by CLI/protocol/TUI/subagents; typed text-block-close, thinking/signature, heartbeat, and four-counter usage events, including signed-thinking accumulation/replacement for replay, liveness-only compaction handling, and input+cache context accounting with zero-frame retention; exact typed HTTP and wire-error classification, six-step jittered backoff ladders, Retry-After clamping, 90-second budget decay, monotonic four-attempt mid-stream protection, heartbeat/first-delta health resets, scheduled-retry deduplication/cancellation, and FIFO queued-turn draining; native mid-session OAuth recovery with refresh-token gating, uncommitted-output protection, scheduled parking, fresh per-launch cancellation signals, concrete HTTP refresh/install, refresh success retry, typed failure teardown, late-result latch clearing, and queued-turn drain; native 120-second streaming-silence watchdog with `Fresh`/`StallFired` deduplication, cancel-token trip, zero-delay synthetic transient error, and two-second slow-tick rebase guard, driven by an active-only 100 ms headless cadence; both executing-tool recovery branches: a 30-second stranded-phase scheduler re-fire and a 330-second hung-worker failure with late-result discard; native partial-JSON closing and required-field guards, two-attempt same-context upstream-truncation recovery, retained block-close truncation signals, and non-retryable `max_tokens` protection; immutable wire-only compaction with latest-summary substitution, 65% compaction/95% normal soft ceilings, exact summary prompt, off-transcript bounded buffering, compaction-specific retry/cancel/queue behavior, persistence effects, post-turn auto-trigger, rapid-refill breaker, and quiet-turn re-enable; loop breaker and immutable re-leak reducer; concrete filesystem persistence port whose loop lifecycle drains submitted effect workers before flushing and stopping the coalescing writer; sealed, call-time-resolved live-provider adapter for Anthropic/OpenAI/Ollama | foundational headless turns, live HTTP provider turns, tool continuations, transient/rate-limit/stall/auth recovery, tool-wedge recovery, truncated tool-input recovery, thinking replay, cache-aware usage, manual/automatic compaction, and persistence shutdown execute end-to-end; application-root settings/auth/tool composition, remaining top-level message domains, and canonical differential traces remain | partial |
| ACP | pinned `acp-cpp` codecs, `acp_methods_test.py`, lifecycle and duplex portions of `acp_integration_test.cpp`/`async_prompt.cpp`, and `src/acp/server.cpp` | `AcpJsonRpcServerTest`, `AgentSessionFactoryTest`, `AcpCommandTest`, `AjentCliTest` | newline-delimited JSON-RPC v2 dispatcher with a duplex, thread-safe stdio connection; exact v1 initialize capability lattice; authenticate/logout; durable session index plus new/list/cwd-filter/load/resume/close/delete; Ask/Write/Minimal modes and call-time model selection; model config option; real asynchronous `AgentLoop` prompt turns with text/resource projection, deferred responses, provider continuation, usage updates, permissioned tool execution, exact outbound `session/request_permission` options, pending/metadata/in-progress/final tool cards, rejection, max-token/refusal/cancel stop mapping, per-session cancellation, concurrent-session isolation, active-prompt ownership, EOF cancellation, and durable completion; ordered `current_mode_update` notification-before-response; thread-store disk reload; idempotent deletion; parse/invalid-request/invalid-params/method-not-found/auth/internal error mapping; notification response suppression and best-effort corrupt/unwritable sidecar recovery; native-bounded load replay; production per-session composition of the selected live provider, profile-derived catalog permissions, all 23 tools, durable thread writer, context ceiling, prompt/memory/skills backends, and native distinction between authenticate credentials and keyless-local prompt authorization; top-level Java 25 `acp` stdio routing with protocol-clean stdout and native readiness diagnostics | source codecs, offline Python method assertions, real reducer/provider/tool turns, duplex permission/cancel/concurrency cases, startup/settings/auth/profile cases, and compiled Java 25 lifecycle smoke are translated; raw native prompt differential capture remains | partial — client filesystem/terminal callbacks and differential edge cases remain |
| MCP client/server | AgenTTY `src/mcp/serve.cpp`/`bridge.cpp`, native tool/effect projection and config trust rules, `src/mcp/http_server.cpp`, and pinned `mcp-cpp` protocol/loopback codecs | `McpJsonRpcServerTest`, `NativeToolWireCatalogTest`, `McpServeCommandTest`, `McpConfigLoaderTest`, `McpClientSessionTest`, `McpStdioTransportTest`, `McpHttpTransportTest`, `McpRegistryTest`, `McpConnectionPoolTest` | newline-delimited MCP JSON-RPC stdio server; initialize negotiation and Ajent implementation metadata/instructions; ping and initialized notification; complete 23-tool standalone catalog with exact native schemas/descriptions and source-only `repo_map`, fail-fast catalog validation, native recall-bias ordering, and MCP-provider annotation semantics distinct from runtime permission effects; production CLI composition with explicit/default workspace resolution, invalid-directory exit parity, `AGENTTY_DOCS_DIR`/local docs discovery, protocol-only stdout, native sandbox diagnostics/strict failure and real process wrapping, real tools/list, and filesystem-changing tools/call; empty-output sentinel; structured file-change summary; tool-level `isError` content; protocol parse/request/params/method/internal errors and notification suppression; lazy config resolution with explicit → project → user precedence, project-local arbitrary-command opt-in gate, `mcpServers`/legacy `servers`, URL-over-command selection, stdio args/environment and HTTP header scalar coercion, and native call/connect timeout defaults; transport-neutral client handshake and initialized notification; bounded pagination and immutable cached discovery for tools/resources/templates/prompts; tool calls with raw/structured/non-text preservation; text/blob resource flattening; role-tagged prompt rendering; live tools/resources/prompts list-changed refresh with generation tracking; concrete spawned-process stdio transport with UTF-8 line framing, concurrent response correlation, notification routing, inbound-request rejection, typed protocol errors, bounded request cancellation, malformed-frame tolerance, stderr draining, remote-close detection, and deterministic process shutdown; concrete JDK Streamable HTTP transport with one POST per frame, JSON/SSE and batch decoding, SSE multi-line/CRLF tolerance, custom headers, MCP session capture/replay and 404/410 expiry, post-initialize protocol version, typed HTTP/protocol/timeout errors, live notification routing, 64 MiB response cap, and in-flight cancellation on close; live multi-provider registry with native collision-only `mcp:<server>__<name>` namespacing, ambiguous bare-name rejection, conservative annotation-to-effect projection, rich structured/image/audio rendering, generic resource/prompt list-and-read tools, resource-owner and prompt routing, live generation propagation, and idempotent all-session shutdown; config-to-session composition selects stdio/HTTP transports, connects every server concurrently on virtual threads behind one global deadline, preserves per-server diagnostics, keeps fast successes, and disposes late sessions without delaying startup | native server behavior, exact wire inventory, compiled CLI launch, real tool execution, platform sandbox lifecycle, adversarial envelopes, configuration security boundary, common client capability semantics, both concrete client transports, registry/application projection, and bounded parallel connection semantics are translated; standalone `task` still needs the provider-backed subagent runner | partial — provider/subagent/TUI composition remains |
| Terminal/rendering | model-label/composer-edit suites, composer key-routing table, Maya `test_input.cpp`/`terminal/input.cpp`, generated Unicode 16 width tables plus `test_unicode_width.cpp`, viewport witness `wire_row.hpp`/`test_wire_row.cpp`, `style/color.hpp`, renderer `StylePool`, packed `Cell`, `Canvas`, `serialize.cpp`, typed `inline_frame.hpp`/`frame_bytes.hpp`, deterministic `test_fresh_oversized_scrollback.cpp` and `scrollback_prefix_harness.cpp` emulators, plus inline lifecycle/restoration sequences from `terminal.hpp` | `ModelLabelsTest`, `ComposerEditorTest`, `ComposerKeyRouterTest`, `TerminalInputDecoderTest`, `UnicodeWidthTest`, `WireViewportTest`, `PackedCellTest`, `TerminalStyleTest`, `TerminalStylePoolTest`, `TerminalCanvasTest`, `CanvasSerializerTest`, `InlineFrameRendererTest`, `OversizedScrollbackOracleTest`, `ScrollbackWirePropertyTest`, `JLineTerminalSessionTest` | labels, immutable composer primitives and exact key routing; complete bounded input FSM including OSC clipboard protocols; exact 122-range East Asian Wide/Fullwidth lookup with Modern-only regional-indicator width and code-point-safe string measurement; viewport-provenanced row values with closed saturating arithmetic, cross-viewport rejection, and exact minimal relative cursor wire emission; bit-identical 64-bit cells and wide-pair markers; immutable composable ANSI/indexed/RGB/default styles, native palette degradation, compact stable interning, reset-prefixed cached SGR, stale-ID fallback, pool-generation invalidation, identity-only faint styling, default-color occlusion semantics, and minimal known-state SGR transitions; packed-cell canvas with value-gated row epochs, Unicode-wide paired writes, nested clipping, clipped-blit orphan repair, equality-gated cached blits, exact content extents, frozen-prefix/tail-capped clears, damage, resize, and paint/drain lifecycle; canvas-bound content-row witnesses, exact full-row DECAWM/SGR/EL serialization, wide-trail suppression, row slicing, packed first/last difference scans, and wide-pair edge snapping; sealed Empty/Fresh/Synced/Stale/HardReset/Sealed type-state, canvas-bound and generation-bound single-use witnesses/proofs/markers, shadow hashing, prefix validation, atomic delivery-before-state-advance, stale soft recovery, synchronized hard-reset wipe, no-op cursor reassertion, terminal-mode finalization, immutable-scrollback first-row gating, persistent DECAWM, minimal changed cell spans, wide-pair edge expansion, scroll-off full-row protection, safe EL, growth cursor movement, shrink re-anchor/erase, and compat full-row repaint; translated ANSI viewport/scrollback emulator proving oversized startup ordering, the bare-fresh duplicate hazard, hard-reset safety, mismatched-prefix escalation, the old unconditional-commit regression, four native shape matrices, and deterministic randomized append/live-tail walks with an append-only scrollback-prefix oracle and per-row uniqueness; JLine 3.30.0 FFM-backed system terminal ownership for JDK 25; raw-mode entry with exact bracketed-paste, KKP, modifyOtherKeys, and cursor setup; raw byte output without Windows newline rewriting; idempotent optional SGR mouse/alt-scroll/focus lifecycle; synchronized-output frame bracket; typed size and WINCH delivery; incremental reads; exact reverse cleanup ordering with DECAWM/cursor/SGR restoration, cooked attributes, and idempotent close | complete pinned input, Unicode-width, viewport witness, cell-layout, public style, production style-wire, canvas mutation, deterministic serialization, native inline type-state transitions, sparse cell-change, append-growth bytes, fresh-oversized and prefix/property scrollback oracle assertions plus inline mode escape order translated; the Java width ranges compare byte-for-value with AgenTTY's generated Unicode 16 table; deterministic JLine terminal tests prove raw entry, resize, reads, synchronized frames, active-mouse teardown, exact cleanup bytes, and closed-state rejection; full reactor coverage gate green | partial — full agent view/runtime scrollback oracle and reveal suites, modals, and live interactive loop remain |
| Interactive UX and platform integration | `runtime/picker.hpp`, `runtime/command_palette.hpp`, `runtime/login.hpp`, `app/update/palette.cpp`, pure login arms from `app/update/login.cpp`, and model/provider arms from `app/update/picker.cpp` | `CommandPaletteTest`, `LoginModalTest`, `ModelProviderPickerTest` | sealed cursor-bearing/cursor-free picker shapes; exact ordered command catalog; shared ASCII filtering; immutable command palette reducer with filtered-row dispatch; sealed seven-state login flow with UTF-8 byte-offset editing and typed browser/OAuth/key/provider/custom-host actions; model picker with Unicode filter, wrap, 14-row jumps, real-index selection and favorite toggling; provider picker with active-row positioning and the virtual custom-host handoff | command-palette, pure login, and pure model/provider picker transitions are ported; modal rendering, action interpretation, model-fetch reconciliation/effort cycling, exchange-result installation, remaining diff/tool-output reducers, and the live loop remain | partial |
| Documentation/distribution | inventory pending | planned | planned | missing | planned |

The terminal input boundary tests additionally pin AgenTTY's 256-byte CSI and
16 MiB OSC/bracketed-paste limits. They prove that invalid UTF-8 continuations
and paste-overflow bytes are reprocessed in the ground state, while the byte
that overflows CSI or OSC is discarded with the partial sequence.
Differential edge assertions also cover the 50 ms bare-Escape timeout,
unknown-SS3 suppression, horizontal-wheel reports, zero and extra mouse
fields, saturating/private CSI parameters, and AgenTTY's padding-terminated
base64 decoder.

The construction plan in `plans/ajent-agentty-java-port.md` is the dependency
and exit-gate authority for advancing these rows.
